'use client'
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    darkPurple: '#242038',
    lightPurple: '#9067c6',
    intermediate: '#8d86c9',
    customGray: '#cac4ce',
    cream: '#f7ece1',
    background: '#efefef',
    shadow: 'rgba(186, 186, 186, 0.3)'
  }
})

export default theme